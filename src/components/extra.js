
                /* Experience Section */
                <div id="experience" className={`section ${experienceInView ? 'section-visible' : ''}`} ref={experienceRef}>
                    <h2>Experience</h2>
                    <p>Details about your experience...</p>
                </div>

                /* Resume Section */
                <div id="resume" className={`section ${resumeInView ? 'section-visible' : ''}`} ref={resumeRef}>                    <h2>Resume</h2>
                    <p>Link or content of your resume...</p>
                </div>

                /* Contact Section */
                <div id="contact" className={`section ${contactInView ? 'section-visible' : ''}`} ref={contactRef}>                    <h2>Contact</h2>
                    <p>How to contact you...</p>
                </div>     